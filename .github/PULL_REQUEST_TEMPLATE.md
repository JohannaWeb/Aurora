## Summary

<!-- What does this PR do? One or two sentences. -->

## Phase reference

<!-- Which phase task(s) does this close or advance? Link the phase file and checkbox. -->

## Changes

<!-- Bullet list of what changed and why. Focus on the why — the diff shows the what. -->

## Test plan

- [ ] `cargo test` passes
- [ ] `make check-line-cap` is clean (no source file over 200 lines)
- [ ] Visual regression snapshots updated if rendering changed (`make update-snapshots`)
- [ ] Manually tested with `cargo run -- <url or --fixture>` if behaviour changed

## Screenshots / output

<!-- If this affects rendering, paste a before/after screenshot or debug dump. -->

## Breaking changes

<!-- Does this change any public API, serialization format, or flag behaviour? If yes, describe the impact. -->
